package com.mycompany.mavenproject23;

/*3-Contador Simple: Crea una clase Contador con un método incrementar() 
que incremente una variable y la muestre.*/
public class Mavenproject23 {

    public static void main(String[] args) {
        Contador contador = new Contador(); // Crear una instancia de Contador

        // Incrementamos varias veces el contador
        contador.incrementar();
        contador.incrementar();
        contador.incrementar();
    }
}
